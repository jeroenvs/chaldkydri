<% div_for post do %>

	<h3><%= link_to_unless_current h(post.title), post %></h3>	
	<%= textilize post.body %>
	<small>Posted by <%= link_to h(post.user.login), post.user %> <%= time_ago_in_words(post.created_at) %> ago</small>

<% end %>